import { styled } from '@/styles'

export const Container = styled('div', {
  cursor: 'pointer',
  display: 'flex',
  flexWrap: 'wrap',
  borderRadius: 8,
  alignItems: 'center',
  justifyContent: 'flex-start',
  minWidth: '100%',
  gap: '0.5rem',
})
